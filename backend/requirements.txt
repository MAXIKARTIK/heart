# FastAPI service runtime dependencies.
# The shared ML library (heart_ml) is installed separately from the project
# root (`pip install -e .`) and pulls in scikit-learn / pandas / numpy / joblib.
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30
pydantic>=2.7
pydantic-settings>=2.3
SQLAlchemy>=2.0
alembic>=1.13
celery>=5.4
redis>=5.0
psycopg2-binary>=2.9
httpx>=0.27

# Test dependencies (mirror the `test` extra in the root pyproject.toml).
# httpx above already provides the Starlette/FastAPI TestClient transport.
pytest>=8.0
pytest-cov>=5.0
hypothesis>=6.100
